services:
  api:
    image: ghcr.io/school-of-company/dovi-ai-server:latest
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    develop:
      watch:
        - action: sync
          path: ./app
          target: /app/app
